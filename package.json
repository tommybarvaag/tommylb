{
  "name": "tommylb",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "cross-env NODE_ENV=dev node server.js && yarn netlify-lambda serve functions",
    "build": "next build",
    "start": "next start",
    "export": "next export",
    "deploy": "yarn build && yarn export"
  },
  "dependencies": {
    "@sendgrid/mail": "^6.4.0",
    "cross-env": "^6.0.3",
    "next": "^9.1.2",
    "react": "^16.11.0",
    "react-dom": "^16.11.0",
    "netlify-lambda": "^1.6.3"
  },
  "devDependencies": {
    "express": "^4.17.1",
    "http-proxy-middleware": "^0.20.0"
  }
}
