# Implementation Plans

Generated by the improve skill (036: `plan` variant 2026-07-03; 037–038:
`branch` audit of `feat/pdf` 2026-07-03 at `556e47c`). Numbering is monotonic
across sessions; plans 001–035 lived in earlier, since-removed instances of
this directory. Each executor: read the plan fully before starting, honor its
STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title                                                    | Priority | Effort | Depends on | Status |
|------|----------------------------------------------------------|----------|--------|------------|--------|
| 036  | Downloadable single-page A4 CV PDF (react-pdf)           | P2       | M      | —          | DONE (2026-07-03: executed, review APPROVED, landed in `556e47c`; prod deploy check of font tracing still pending) |
| 037  | Cache the CV PDF response + pin the route contract       | P1       | S      | —          | DONE, reconcile-verified 2026-07-03: every hunk traces to a plan step; all done criteria re-run by reviewer incl. live dev-curl (`no-cache, no-store`); uncommitted on `feat/pdf` for owner commit |
| 038  | Parse CV date strings as local calendar dates (TZ-proof) | P2       | S      | —          | DONE, reconcile-verified 2026-07-03: diff matches plan exactly, `cv-data.test.ts` untouched, TZ matrix UTC/Oslo/NY re-run green by reviewer; uncommitted on `feat/pdf` for owner commit |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 037 and 038 are independent (no file overlap) and can run in parallel or
  either order. Both stamp `556e47c`.

## Findings considered and rejected

- Client-side PDF generation (`PDFDownloadLink`): rejected — ships the whole
  react-pdf runtime to the browser for a static document; server render keeps
  client JS at zero.
- `serverExternalPackages` config for react-pdf: unnecessary — the package is
  on Next.js's built-in auto-externalized list (verified in bundled docs).
- "Render test would pass a blank PDF" (branch-audit TEST-01, part 1):
  rejected — a blank render embeds no font glyphs and lands ~1–2 KB, failing
  the existing `> 10_000` size assertion. The residual (no targeted content
  assertion) is folded into plan 037 as the UTF-16BE title check.
- UTC-normalizing the date formatters (branch-audit BUG-01 fix sketch):
  rejected approach — existing characterization tests construct local dates
  and would break east of UTC. Plan 038 fixes the root cause at the parse
  boundary instead.
- Route handler DoS beyond caching (rate limiting): not worth doing on a
  personal site once 037's edge caching + per-instance memo land.
- `pnpm audit --prod` at `556e47c`: no known vulnerabilities — dependency
  posture clean, nothing to plan.
