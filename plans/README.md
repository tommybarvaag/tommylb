# Implementation Plans

Generated by the improve skill on 2026-07-03 (`plan` variant — single
user-requested feature, no audit). Numbering is monotonic across sessions;
plans 001–035 lived in earlier, since-removed instances of this directory.
Each executor: read the plan fully before starting, honor its STOP conditions,
and update your row when done.

## Execution order & status

| Plan | Title                                                | Priority | Effort | Depends on | Status |
|------|------------------------------------------------------|----------|--------|------------|--------|
| 036  | Downloadable single-page A4 CV PDF (react-pdf)       | P2       | M      | —          | DONE (2026-07-03: executed in worktree `agent-af0612e2e11c34dd2`, review APPROVED — all done criteria re-verified by reviewer incl. visual PDF check; awaiting owner to apply the worktree diff to `feat/pdf`) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- None. 036 is self-contained.

## Findings considered and rejected

- Client-side PDF generation (`PDFDownloadLink`): rejected — ships the whole
  react-pdf runtime to the browser for a static document; server render keeps
  client JS at zero.
- `serverExternalPackages` config for react-pdf: unnecessary — the package is
  on Next.js's built-in auto-externalized list (verified in bundled docs at
  commit db84027).
